services:
  web:
    build: .
    container_name: rni_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./src:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - data_stack_net

networks:
  data_stack_net:
    external: true

